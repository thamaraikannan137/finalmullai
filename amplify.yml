version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npm install -g npm@10.9.2
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - .npm/**/*
      - node_modules/**/*
      - .next/cache/**/*
